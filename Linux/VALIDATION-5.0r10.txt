WaffleHouse-Client 5.0r10 validation

- Fix baseline: WaffleHouse-Client 5.0r9 desktop tree.
- PASS: macOS framework-link regression test converts pkg-config `-framework;Name` pairs into linker framework options.
- PASS: `CoreServices` does not remain in the normal-library list and therefore cannot become `-lCoreServices`.
- PASS: normalized framework options are attached to the final application target, not the static SIP core archive.
- PASS: unused `this` capture removed from Search History clear-button lambda.
- PASS: existing Asterisk chan_sip compatibility regression retained.
- PASS: shell builders pass `sh -n`.
- Native macOS link must still be verified on an actual macOS host; this Linux validation cannot execute AppleClang/ld.
